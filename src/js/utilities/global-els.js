// use this file to store commonly referenced nodes (save from having to hit the dom unnecessarily)

var globalEls = {
    win: window,
    doc: document,
    html: document.documentElement
}

export { globalEls }
